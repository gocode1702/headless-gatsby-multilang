import React from 'react';
import PageWrapper from '../components/layout/pageWrapper';

const CategoryTemplate = ({ pageContext }) => {
  return <PageWrapper pageData={pageContext}>Ciao</PageWrapper>;
};

export default CategoryTemplate;
